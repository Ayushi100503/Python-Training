# Python Training exercises

This repository contains Python training files created during learning an d practice sessions.

## Structure

- Each folder is named by the *date*(`DD.MM.YYYY`) on which the training was done
- Inside each folder are one or more Python files covering concepts practiced that day and docx files containing explanations of the tech stacks used.